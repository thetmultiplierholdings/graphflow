# Scenario 4 — the guardrails — expected outcome

> Generated by `DemoScenario.test.ts` from the matching `_input.md`. Do not hand-edit —
> regenerate with `GRAPHFLOW_UPDATE_DEMOS=1 npm run test -- src/demo/DemoScenario.test.ts`.

## Executions

Node lists are SORTED here: `Summary` reports them in activity-completion order, and the
workflows run their document chains through `Promise.all`, so the live order varies run to
run — the multiset (which node ids, how many times) is the contract, not the order.

### execute jan

```
node bodies EXECUTED :  5  ['append_to_master', 'build_report', 'calculate_tax', 'ocr_brokerage_statement', 'verify_txns']
memo HITS            :  0  []
human questions asked:  1  ['verify_txns']
```

## Workflow runs (read straight from the db)

| run | id | workflow | lineage_kind | state | copied_from | root | lineage_byid | lineage_display | user docs | engine results |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| jan | 1 | tax_demo_workflow | root | frozen | — | jan | 1 | January estimate | 1 | 5 |
| draft | 2 | tax_demo_workflow | root | draft | — | draft | 2 | March — still gathering documents | 1 | 0 |

## Engagements

| engagement | workflow runs | artifacts | node runs | human answers |
| --- | --- | --- | --- | --- |
| acme | 2 | 8 | 5 | 1 |

## Rejected commands

### fail upload jan brokerage_statement extra_ubs.txt

```
workflow run 1 is frozen (already executed) — attachments can no longer change; create a copy or revision
code: RUN_FROZEN
```

### fail detach jan brokerage_statement

```
workflow run 1 is frozen (already executed) — attachments can no longer change; create a copy or revision
code: RUN_FROZEN
```

### fail execute jan

```
workflow run 1 has already completed — create a copy or revision to run it again
code: RUN_FROZEN
```

### fail run nope1 = copy draft revision "revision of a draft"

```
workflow run 2 has never been executed — only finished runs can be copied
code: RUN_NOT_COPYABLE
```

### fail run nope2 = copy jan revision "cross-workflow revision" workflow=tax_demo_workflow_v2

```
a revision must keep the parent's workflow 'tax_demo_workflow' — asking for a different workflow is a copy
```

### fail run nope3 = copy jan root "root with a parent"

```
lineage_kind 'root' cannot carry copy_from — use 'copy', 'revision' or 'simulation'
```
